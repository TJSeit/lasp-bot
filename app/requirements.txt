# App dependencies — installed in the Docker container
# Pin major versions to keep the image reproducible

# Web framework
fastapi>=0.111,<1
uvicorn[standard]>=0.29,<1

# LangChain ecosystem
langchain>=1,<2
langchain-community>=0.4,<1
langchain-huggingface>=1,<2

# Embeddings (sentence-transformers, CPU inference in the container)
sentence-transformers>=2.7,<4

# FAISS — CPU build (no GPU required)
faiss-cpu>=1.8,<2

# Ollama client
ollama>=0.4,<1

# Environment variable loading
python-dotenv>=1.0,<2

# MCP (Model Context Protocol) server
mcp>=1.0,<2

# HTTP client (used by the MMS SDC MCP tool)
httpx>=0.27,<1

# Discord bot
discord.py>=2.3,<3
